#include<bits/stdc++.h>

using namespace std;

int main()
{
	int t;
	cin>>t;
	
	cout<<fixed<<showpoint;
	while(t--)
	{
		int r,g;
		cin>>r>>g;

		double prob = 1.00000;		
		if(r==0 || g==0)
		{	
			
			cout<<setprecision(5);
			cout<<prob<<endl;
		}
	}
}
